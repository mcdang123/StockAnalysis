# Copy this file to .env and fill in real values. .env is git-ignored.

# MySQL root password shared by all 3 services (fetcher_db, analysis_db, pattern_db all live on one server)
DB_PASSWORD=changeme

# Finnhub API key used by stock_fetcher_service (https://finnhub.io/)
FINNHUB_API_KEY=
